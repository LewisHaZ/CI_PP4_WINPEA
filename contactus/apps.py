# 3rd Party Imports
from django.apps import AppConfig


class ContactusConfig(AppConfig):
    """
    A class to set up the contact us
    app
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'contactus'
